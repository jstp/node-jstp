var vows    = require('vows')
  , assert  = require('assert')
  , jstp    = require('../../index.js');

vows.describe('JSTPCallable').addBatch({
  '#call( JSTPEngine engine, JSTPTriggeringPackage triggeringPackage )': {
    'should accept the arguments': function () { }
  }
}).export(module); 